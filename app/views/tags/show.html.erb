<h3>#<%= @tag.name %></h3>
 <% @tag.links.each_with_index do |link, i| %>
  <tr>
	<td>
		<%= i + 1 %>.	 
	</td>
	<td>
    	<tr><td><%= link_to link.title, link_path(link), class: 'link_title_tag link_title' %></td></tr>
	</td>
  </tr>
<% end %>
