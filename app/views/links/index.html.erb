<!DOCTYPE html>
<html>
	<body>
	  	<div class="feed_container">
	  		<table bgcolor="#ecf0f1">
	  			<tbody>
	  			<tr>
	  				<table bgcolor="#3498db" style="width: 100%; color: #fff">
	  					<tr>
			  				<span class="nav_links"><%= link_to "Submit", link_path("new"), class: 'visited'%></span>
			  				<span class="nav_links"><%= link_to "Create", group_path("new"), class: 'visited'%></span>
			  				<span class="nav_links"><a href="#" class="visited">Search</a></span>
			  				<span class="nav_links"><%= link_to "Messages" , class: 'visited'%></span>
			  				<span class="nav_links"><%= link_to "Settings", class: 'visited'%></span>
			  			</tr>
	  				</table>
	  			</tr>
	  			<tr style="height:5px"></tr>
	  			<tr>
	  				<table>
	  					<% @link.each do |link| %>
	  					<tr>
		  					<td>
		  						score
		  					</td>
		  					<td>
		  						<a href=<%= link.url %> class="link_title_tag link_title"><%= link.title %></a>
		  					</td>
	  					</tr>
	  					<% end %>
	  				</table>
	  			</tr>
	  			</tbody>
	  		</table>
	  	</div>
	 </body>
</html>