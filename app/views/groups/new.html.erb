<%= form_for(@group) do |f| %>
  
  	<% @group.errors.full_messages.each do |error| %>
    
	<%= error %>

<% end %>
  
	    <div>
	    	<%= f.text_field :name , class: 'title_box' , placeholder: 'group name' %>
	    </div>
	    <div>
	    	<%= f.text_area :desc , class: 'title_box' , placeholder: 'description' %>
	    </div>
	    <%= f.submit %>

<% end %>
